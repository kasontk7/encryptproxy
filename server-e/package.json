{
  "name": "server-e",
  "version": "1.0.0",
  "description": "a server that reads files from disk, encrypts them, and uploads them to server M",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mime": "^2.4.3"
  }
}
